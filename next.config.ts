/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true, // If you're having optimization issues
    domains: [], // Add any external domains if needed
  },
}

module.exports = nextConfig